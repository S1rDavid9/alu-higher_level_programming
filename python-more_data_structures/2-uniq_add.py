#!/usr/bin/python3
def uniq_add(my_list=[]):
    # Create a set to store unique elements
    unique_elements = set(my_list)
    # Sum the elements in the set
    total = sum(unique_elements)
    return total
