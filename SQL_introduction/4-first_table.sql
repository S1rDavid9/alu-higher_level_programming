-- This creates a table called first_table in the current database in your MySQL server
-- Not allowed to use the SELECT or SHOW statements

CREATE TABLE IF NOT EXISTS first_table (
    id INT,
    name VARCHAR(256)
);
